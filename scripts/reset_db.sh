#!/bin/bash
# Reset database script for Multi-Agent Earnings Analyzer
# Note: This application doesn't use a persistent database, it processes files on-demand
# This script is a no-op for compatibility with E2E test infrastructure

echo "Database reset completed (no persistent DB in use)"
exit 0
